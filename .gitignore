# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
dist/
build/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Blender backups (если случайно положишь .blend рядом)
*.blend1
*.blend2

# Локальные секреты (на всякий случай)
.env
.env.*
*.pem
credentials.json

# Cursor — правила проекта оставляем в репо (агенту нужны)
# .cursor/

# Слепки и kit — НЕ игнорируем: агенту полезны graph_snapshots/ и authoring_kit/
